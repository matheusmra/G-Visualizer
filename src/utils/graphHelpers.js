/**
 * Graph helper utilities — pure functions with no algorithm-specific logic.
 * Re-exported from their original algorithm modules to keep a single source of truth.
 */

export { buildAdjMap } from '../algorithms/BFS.js';
export { buildDirectedAdjMap, buildReverseAdjMap } from '../algorithms/FTC.js';
